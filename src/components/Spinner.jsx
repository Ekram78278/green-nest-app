import React from 'react';

const Spinner = () => {
    return (
        <div>
            <span className="loading loading-bars loading-xl mx-auto flex flex-col justify-center"></span>
        </div>
    );
};

export default Spinner;